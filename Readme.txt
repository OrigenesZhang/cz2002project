To execute the program:

1. Open the Folder named "Executable"
2. Double click the "Run.bat" file
3. data.dat is the sample data we have created, if you would prefer initializing a default system, simply delete or move data.dat to another directory.

